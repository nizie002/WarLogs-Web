'use client';

// Placeholder for global toast management
// Real implementation will follow with Context/State management
export function useToast() {
    return {
        toast: (msg: string) => console.log('Toast placeholder:', msg),
    };
}
