export default function OpenPlayGame({ sessionId }: { sessionId: string }) {
    return (
        <div>
            <h2>Open Play Game</h2>
            <p>Session: {sessionId}</p>
            {/* TODO: Implement game flow */}
        </div>
    );
}
